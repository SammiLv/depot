# 2026-08-21 产品管理改版 — 环境准备与执行计划交接

## 背景

在 `depot-platform` worktree 上，为**产品管理模块（路由 `/quarterly-work`）改版**准备开发环境，并完成 MasterGo 设计稿解读与执行计划。代码基线已与 `depot-clean/main` 对齐（`3318d44`）。

**Claude Code 会话 ID**：`ba34077c-aadd-47f4-9e6d-d9910c08b1a1`（因会话历史过大 + Kimi API 兼容问题，后续无法 `--continue`；本交接文档从会话记录与仓库状态整理，供新会话直接接手）。

---

## 已完成工作

### 1. 代码与环境同步

| 步骤 | 状态 | 说明 |
|------|------|------|
| 备份 | ✅ | 分支 `depot-platform-pre-sync`；`.env.pre-sync.bak`、`db/dev.db.pre-sync.bak` |
| `git reset --hard main` | ✅ | 当前 HEAD = `3318d44` |
| 重建 dev:platform | ✅ | `./scripts/refresh-env-after-merge.sh dev:platform` |
| 服务 | ✅ | `http://localhost:3003` 监听中 |

### 2. 产品管理测试数据

- **脚本**：`scripts/import-product-demo-data.ts`（**未提交**，当前为 untracked）
- **数据源**：只读 `depot-clean/db/dev.db`
- **执行**：`npx tsx scripts/import-product-demo-data.ts`
- **备份**：导入前 `db/dev.db.pre-product-seed.bak`

**当前库计数（已验证）**：

| 表 | 数量 |
|---|---|
| ProductGoal | 5 |
| Project | 18 |
| QuarterlyWork | 12 |
| RequirementValueTrack | 4 |
| OperationLog | 13 |

脚本从源库复制真实数据并重映射 User/OrgNode，另新建 7 名真实姓名展示用户（无登录权限），并补充「已完成/关闭/延期完成」等合成数据，使看板各状态列有内容。

### 3. 多环境登录 Cookie 隔离

**问题**：localhost 不区分端口，3000 与 3003 共用 cookie 名会互相顶掉登录态。

**处理**：`.env` 增加（**未提交**）：

```env
SESSION_COOKIE_NAME="department_mvp_user_id_3003"
```

`src/server/auth/current-user.ts` 已支持从环境变量读取，无需改代码。改后需重启 3003 服务。

### 4. MasterGo 设计稿解读与改版计划

**设计稿文件**：https://mastergo.com/file/201969861780875

| 稿 | layer_id | MasterGo 名称 | 关键内容 |
|---|---|---|---|
| 卡片模式 | `13:15736` | 产品管理首页/卡片/任务 | 完整首页，右侧为项目卡片流，卡片内展示任务列表 |
| 卡片模式 | `26:4514` | 产品管理首页/卡片/价值跟踪 | 完整首页，右侧项目卡片内切到价值跟踪 |
| 卡片空态 | `26:04348` | 数据为空/价值跟踪 | 单项目卡片，价值跟踪为空，显示「暂无跟踪过程 / 去创建」 |
| 卡片空态 | `26:09548` | 数据为空/任务 | 单项目卡片，任务为空，显示「暂未创建任务 / 去创建」 |
| 卡片空态 | `26:21692` | 数据为空/hover创建按钮 | 任务空态 hover 态，创建按钮高亮 |
| 列表模式 | `13:15239` | 产品管理首页/列表/任务 | 完整首页，右侧为项目树形表格，展开区展示任务 |
| 列表模式 | `26:17231` | 产品管理首页/列表/任务 | 完整首页，右侧项目树形表格，展开区展示价值跟踪 |

**设计稿已确认的页面结构**：

1. 整体画布为 `1440 × 900`，背景 `#F5F7F9`；左侧固定两列：72px 一级图标栏 + 228px 产品管理目标面板，总宽 300px。右侧主内容从 `x=300` 开始，宽 1140px。
2. 左侧目标面板包括搜索「搜索目标、项目、任务」、全部汇总（关联项目 31、项目任务 120）、目标列表（如 AI 应用能力建设、产品运营推广能力建设等）和「操作日志」入口。
3. 主内容顶部高度约 194px：标题为「全部」，右上为「操作日志」；说明文案为「以产品目标为核心管理年度目标、关联项目、项目任务与上线后价值跟踪，每周更新进展，延期自动预警。」；筛选区包括业务组、年份、季度、负责人、项目筛选；状态页签为「全部项目 / 进行中 / 未启动 / 已上线 / 已完成 / 关闭 / 延期」；右侧视图切换为「图表/列表」（产品实现文案建议改为「卡片/列表」）。
4. 卡片模式：工作区为横向项目卡片流，每个项目容器宽 320px、内容宽 288px。项目信息下方有卡片内 Radio Group「任务 / 价值跟踪」，任务卡可显示负责人、周期、工时、剩余周数，价值跟踪卡显示跟踪状态、价值判断、实际收益、跟踪过程。
5. 列表模式：表格宽 1108px，列为「名称 / 所属目标 / 状态 / 负责人 / 周期 / 操作」。项目行为一级行，支持展开；展开区同样在「任务 / 价值跟踪」间切换，任务或跟踪记录作为二级行展示。
6. 空态必须纳入本版：项目没有任务时显示「暂未创建任务」「创建任务，开始你的工作吧！」「去创建」；项目没有价值跟踪记录时显示「暂无跟踪过程」「需求上线后，记得要记录跟踪过程！」「去创建」。

**核心改版方向**：将现有 5 个 tab（产品目标 / 项目看板 / 任务看板 / 需求价值跟踪 / 操作日志）收敛为**以产品目标为导航的单页工作台**：左目标导航 + 右主区（卡片视图或列表视图）。任务与价值跟踪不再作为页面级主 tab，而是成为项目卡片/项目展开行内的二级切换；操作日志作为左侧入口和顶部按钮保留。

**用户已确认决策（2026-08-21）**：

1. **价值跟踪**：设计稿已覆盖，本版进入项目卡片/项目展开行内的二级切换；原页面级「需求价值跟踪」tab 不再作为主入口。
2. **操作日志**：保留为独立查看入口（左侧目标面板 + 顶部按钮），不混入项目树和卡片流。
3. **视图切换**：设计稿文案仍为「图表/列表」，实现时建议展示为「**卡片** / 列表」。
4. **「延期」页签**：指**未完成但已超期**的任务/项目；**沿用现有业务逻辑**（非仅 DELAYED_COMPLETED）。
5. **替换策略**：新版**直接替换**旧版，含左侧产品管理菜单与旧 5-tab 主结构。
6. **本次范围边界**：查看、编辑、删除、新增等二级页面或弹窗设计稿尚未输出；本次只做列表/卡片主页面改版，二级弹窗与表单先沿用现有实现，不按主页面设计稿外推重做。

---

## 未提交变更清单

```text
?? scripts/import-product-demo-data.ts
?? db/dev.db.pre-product-seed.bak
?? db/dev.db.pre-sync.bak
 M .env   # SESSION_COOKIE_NAME（若 git 未 ignore）
```

**建议下一手**：确认测试数据与 cookie 配置无误后，单独 commit 脚本；`.env` 与 `db/*.bak` 不入库。

---

## 改版执行计划（下一手直接实施）

### 阶段 0：Next.js 16 与现状确认

1. 遵守 AGENTS.md：改 `src/app` 前先读 `node_modules/next/dist/docs/` 中与 App Router、searchParams、server/client component 相关文档。
2. 当前产品管理 UI 主要集中在 `src/app/(authenticated)/quarterly-work/content.tsx`（约 3000+ 行），已有页面级 `BoardTab = "goal" | "project" | "board" | "value" | "log"`、`ViewMode = "card" | "list"`。
3. 当前查询集中在 `src/server/quarterly-work/quarterly-work-query.ts`，已有项目/任务/目标/价值跟踪/操作日志数据、权限与延期判定，优先扩展数据形状，避免重写写操作。

**阶段 0 执行记录（2026-08-21 17:03）**：

1. 本地 `node_modules/next/dist/docs/` 未包含可读取文档文件；已补充查阅 Next.js 16 官方文档：
   - [`page` file convention](https://unpkg.com/next@16.3.1/dist/docs/01-app/03-api-reference/03-file-conventions/page.md)：`params` / `searchParams` 在 Next 16 中是 Promise，需 `await` 或用 React `use()` 解包。
   - [`useSearchParams`](https://nextjs.org/docs/app/api-reference/functions/use-search-params)：仅限 Client Component；若静态预渲染页面使用该 hook，需要 Suspense 边界；服务端取参数优先用 Page 的 `searchParams` prop。
   - [`Server and Client Components`](https://nextjs.org/docs/app/getting-started/server-and-client-components)：App Router 默认 Server Component，只有需要交互、hooks、浏览器 API 的入口文件加 `"use client"`。
2. 当前 `src/app/(authenticated)/quarterly-work/page.tsx` 已按 Next 16 正确将 `searchParams` 声明为 `Promise` 并 `await`；目前只解析 `year`、`quarter`，阶段 1 增加筛选参数时继续在服务端 page 中解析。
3. 当前 `content.tsx` 是单个大型 Client Component，内部同时包含页面主导航、5 个旧 tab、卡片/列表切换、筛选、所有表单弹窗和删除确认；阶段 2 应先拆主页面组件，但弹窗内部逻辑只搬迁/复用，不做视觉改版。
4. 当前 `src/components/app-shell.tsx` 与设计稿不一致：现有全局壳是 240px 左侧栏 + 64px 顶部 header，设计稿是 72px 一级图标栏 + 228px 产品管理面板。本次若严格限定为列表/卡片主页面改版，建议暂不改全局 `AppShell`，先在 `/quarterly-work` 内容区内实现目标面板和工作区，并按现有可用宽度自适应；若后续要完全贴稿，需要单独确认全局壳/产品管理专属壳是否纳入范围。
5. `src/server/quarterly-work/actions.ts` 已覆盖目标、项目、任务、价值跟踪的 create/update/delete，权限分为产品目标、项目与价值跟踪、任务三类，且统一写操作日志并 `revalidatePath("/quarterly-work")`；阶段 1 不改写操作，只扩展查询聚合数据。
6. 操作日志当前目标类型为 `PRODUCT_GOAL`、`PROJECT`、`QUARTERLY_WORK`，价值跟踪写入以项目为目标；阶段 5 保留此口径。

### 阶段 1：数据层重塑（`src/server/quarterly-work/`）

1. 在 `quarterly-work-query.ts` 中新增面向新版页面的聚合结构，建议并行保留旧字段到 UI 迁移完成：
   - `goalNavigationItems`：`全部` + 每个 `ProductGoal`，含 `projectCount`、`taskCount`、目标描述、负责人/组织信息。
   - `projectWorkspaceItems`：以 `Project` 为一级节点，内含关联目标、负责人、状态、周期、是否延期、任务列表、价值跟踪摘要与跟踪记录。
   - `workspaceSummary`：关联项目总数、项目任务总数、当前筛选后的数量。
2. 统一筛选口径：
   - URL/searchParams：`goalId`、`view`、`projectPanel`（task/value）、`status`、`orgNodeId/teamId`、`year`、`quarter`、`ownerId`、`projectId`、`q`。
   - 全局搜索覆盖 `ProductGoal.title/description`、`Project.title/description/expectedOutcome`、`QuarterlyWork.title/taskDescription/expectedOutcome`、`RequirementValueTrack.trackingResult/followUpOptimization`。
3. 状态页签按设计稿统一为项目级过滤：「全部项目 / 进行中 / 未启动 / 已上线 / 已完成 / 关闭 / 延期」；任务在项目内部展示，但搜索和延期仍能命中任务。
4. 延期判定沿用现有逻辑：
   - 任务：复用 `formatRemainingWeeksLabel` 与现有 `isWorkOverdue` 口径；进行中/未启动任务显示剩余周数或超期。
   - 项目：复用现有 `isProjectOverdue` 口径，已上线/已完成项目按实际上线/完成时间与计划结束季度比较。
5. `actions.ts`、权限函数与日志写入逻辑保持不变；新增/编辑/删除目标、项目、任务、价值跟踪仍走现有 server actions。

**阶段 1 执行记录（2026-08-21 17:17）**：

1. 已在 `src/server/quarterly-work/quarterly-work-query.ts` 扩展 `getQuarterlyWorkData` 的 `options`，兼容新增筛选参数：`goalId`、`view`、`projectPanel`、`status`、`orgNodeId`、`teamId`、`ownerId`、`projectId`、`query`；旧调用只传 `selectedYear/selectedQuarter` 仍可用。
2. 已追加新版主页面数据结构，不删除旧字段，现有 UI 可继续运行：
   - `goalNavigationItems`：含「全部」与年度目标列表，聚合项目数、任务数、负责人、组织与描述信息。
   - `projectWorkspaceItems`：以项目为一级对象，包含关联目标、项目状态/延期状态、任务列表、价值跟踪列表、价值跟踪摘要。
   - `workspaceSummary`：统计总项目数/筛选后项目数、总任务数/筛选后任务数、价值跟踪数、延期项目数、延期任务数。
   - `workspaceFilters`：返回规范化后的当前筛选值，供阶段 2 UI 直接消费。
3. 全局搜索已在数据层预留，覆盖目标、项目、任务和价值跟踪文本字段；阶段 2 接入 UI 时只需传 `query`。
4. 项目/任务延期仍复用原查询里的 `isProjectOverdue`、`isWorkOverdue` 口径；写操作、权限、操作日志未改。
5. 校验结果：
   - `npx eslint "src/server/quarterly-work/quarterly-work-query.ts"` 通过。
   - `npx tsc --noEmit` 通过。
   - `npm run lint` 全量仍失败，原因是仓库既有 lint 问题（脚本、KPI、组织、通知、旧产品管理 UI 等文件），本次改动文件已单独通过。

### 阶段 2：页面骨架与状态迁移

拆分现 `content.tsx`，先迁移骨架，再迁移业务卡片/行：

```text
quarterly-work/
  content.tsx                         # 只保留状态编排、弹窗编排和数据分发
  components/
    quarterly-work-shell.tsx           # 300px 左区 + 1140px 右区主布局
    goal-navigation-panel.tsx          # 搜索、全部汇总、目标列表、操作日志入口
    workspace-header.tsx               # 标题、说明、操作日志按钮
    workspace-filters.tsx              # 业务组/年份/季度/负责人/项目
    workspace-status-tabs.tsx          # 状态页签 + 卡片/列表切换
    project-card-board.tsx             # 320px 项目卡片流
    project-card.tsx                   # 项目信息 + 任务/价值跟踪内切换
    project-tree-table.tsx             # 1108px 树形表格
    project-expanded-panel.tsx         # 列表展开区任务/价值跟踪切换
    empty-project-section.tsx          # 任务空态、价值跟踪空态
    countdown-tag.tsx
    dialogs.tsx                        # 仅搬迁/复用现有表单弹窗，不做视觉改版
```

实施要点：

1. 先把旧页面级 5-tab 主导航移除，改为单页工作台；保留现有弹窗表单组件，避免一次性重写增删改。
2. 页面状态优先落到 URL searchParams，刷新可还原；本地 state 只保留弹窗开关、当前展开项目、卡片内二级切换等即时交互。
3. 设计稿的 72px 一级图标栏由现有 `app-shell` 提供；产品管理页内部实现 228px 目标面板和右侧工作区即可，不重复实现全局侧边栏。
4. 所有查看、编辑、删除、创建入口仅调整触发位置和默认值；弹窗内部字段、校验、布局和交互不纳入本次主页面改版范围。

**阶段 2 执行记录（2026-08-21 17:21）**：

1. 已扩展 `src/app/(authenticated)/quarterly-work/page.tsx` 的 `searchParams` 解析，向数据层传入 `goalId`、`view`、`projectPanel`、`status`、`orgNodeId`、`teamId`、`ownerId`、`projectId`、`q`。
2. 已在 `content.tsx` 中新增新版 `QuarterlyWorkShell`：
   - 左侧 228px 目标导航面板，接入 `goalNavigationItems` 和全局搜索。
   - 右侧工作区顶部接入部门/小组/年份/季度/负责人/项目筛选。
   - 状态页签统一为「全部项目 / 进行中 / 未启动 / 已上线 / 已完成 / 关闭 / 延期」。
   - 卡片/列表切换已落到 URL `view`，当前先提供阶段 2 骨架级项目卡片和列表行。
   - `workspaceSummary` 已用于展示项目、任务、价值跟踪、延期统计。
3. 已移除旧页面级 5-tab 主导航的实际渲染路径；旧实现暂留在文件内作为后续阶段迁移参考，当前主页面默认渲染新版 shell。
4. 已复用现有新增产品目标、新增项目、新增任务、新增价值跟踪、编辑项目、删除项目、操作日志弹窗入口；弹窗内部未做视觉重写。
5. 校验结果：
   - `npx eslint "src/app/(authenticated)/quarterly-work/page.tsx" "src/app/(authenticated)/quarterly-work/content.tsx"` 通过。
   - `npx tsc --noEmit` 通过。
6. 阶段 3/4 待继续：按设计稿精细实现 320px 项目卡片、任务/价值跟踪内切换、树形列表展开区与空态视觉。

### 阶段 3：卡片模式实现

1. 工作区使用横向滚动项目卡片流：项目容器宽 320px，卡片内内容宽 288px，卡片间距约 16px。
2. `ProjectCard` 顶部展示项目标题、描述、负责人头像/姓名、所属目标，使用项目状态决定渐变/标签色。
3. 卡片内二级切换固定为「任务 / 价值跟踪」：
   - 任务态：展示任务标题、任务目标/说明、负责人、周期、工时、剩余/超期标签和编辑入口。
   - 价值跟踪态：展示跟踪状态、价值判断、实际收益、跟踪过程记录和编辑入口。
4. 空态按设计稿落地：
   - 无任务：`暂未创建任务`、`创建任务，开始你的工作吧！`、`去创建`，按钮打开新增任务弹窗并预选当前项目。
   - 无价值跟踪：`暂无跟踪过程`、`需求上线后，记得要记录跟踪过程！`、`去创建`，按钮打开新增价值跟踪弹窗并预选当前项目。
5. 注意 MasterGo 中部分样例文本标记为 placeholder，真实实现必须使用数据库字段，不复制样例长文。

**阶段 3 执行记录（2026-08-21 17:28）**：

1. 已在 `content.tsx` 中实现卡片模式核心组件：
   - `ProjectCard`：320px 横向卡片，展示项目标题、说明、负责人、所属目标、周期、项目状态/延期标签。
   - `CountdownTag`：展示任务剩余/超期标签，复用阶段 1 数据层的延期口径。
   - `ProjectTaskCard`：展示任务标题、说明/目标、负责人、状态、月份周期、工时、编辑入口。
   - `ProjectValueTrackCard`：展示价值跟踪季度、跟踪时间、跟踪结果、后续优化、编辑入口。
   - `EmptyProjectSection`：复用任务空态和价值跟踪空态。
2. 卡片内二级切换已接入 URL `projectPanel`：
   - `task` 为默认任务态。
   - `value` 为价值跟踪态。
3. 空态文案已按设计稿要求落地：
   - 无任务：`暂未创建任务`、`创建任务，开始你的工作吧！`、`去创建`。
   - 无价值跟踪：`暂无跟踪过程`、`需求上线后，记得要记录跟踪过程！`、`去创建`。
4. 创建/编辑入口已复用现有弹窗：
   - 空态和卡片底部的新增任务会预选当前项目。
   - 空态和卡片底部的新增价值跟踪会预选当前项目。
   - 任务编辑、价值跟踪编辑、项目编辑/删除仍走现有表单和 server actions。
5. 校验结果：
   - `npx eslint "src/app/(authenticated)/quarterly-work/content.tsx"` 通过。
   - `npx tsc --noEmit` 通过。

### 阶段 4：列表模式实现

1. 表格总宽按 1108px 设计，列宽参考 DSL：名称约 396/502px、所属目标 120px、状态 60px、负责人 100/120px、周期 160px、操作 100px。
2. 一级项目行支持展开/收起，操作列保留编辑/删除/新增任务或价值跟踪入口，权限沿用现有按钮显隐。
3. 展开区提供「任务 / 价值跟踪」二级切换：
   - 任务展开：显示任务标题、说明/目标、状态、负责人、月份/周期、操作。
   - 价值跟踪展开：顶部显示跟踪状态、价值判断、实际收益摘要，下方展示跟踪过程记录（季度、结果、后续优化、状态、负责人、周期、操作）。
4. 若项目下无任务或无价值跟踪，复用阶段 3 的空态组件，保持卡片/列表体验一致。

**阶段 4 执行记录（2026-08-21 17:31）**：

1. 已在 `content.tsx` 中实现列表模式核心组件：
   - `ProjectTreeTable`：1108px 最小宽度项目表格，列包含项目名称、所属目标、状态、负责人、周期、操作。
   - `ProjectExpandedPanel`：一级项目行展开区，复用阶段 3 的 `EmptyProjectSection`。
2. 一级项目行已支持展开/收起；初始默认展开当前筛选结果的第一个项目，用户可收起。
3. 展开区二级切换已接入 URL `projectPanel`：
   - 任务态显示任务名称、说明/目标、状态、负责人、月份/周期、工时/剩余或超期标签、编辑入口。
   - 价值跟踪态显示跟踪状态、价值判断、实际收益摘要，以及季度、跟踪结果、后续优化、状态、负责人、编辑入口。
4. 列表操作入口已接入现有弹窗和权限：
   - 项目行保留新增任务、新增跟踪、编辑项目、删除项目。
   - 展开区保留任务编辑、价值跟踪编辑。
   - 无任务/无价值跟踪时复用阶段 3 空态和预选当前项目的创建入口。
5. 校验结果：
   - `npx eslint "src/app/(authenticated)/quarterly-work/content.tsx"` 通过。
   - `npx tsc --noEmit` 通过。

### 阶段 5：操作日志与弹窗整合

1. 操作日志保留独立入口：左侧目标面板的「操作日志」与主内容顶部「操作日志」按钮均打开同一日志视图/弹窗。
2. 保留现有 `OperationLogDialog`、目标/项目/任务/价值跟踪创建编辑删除表单；只调整触发入口、默认项目/目标、成功后的局部状态刷新。
3. 查看、编辑、删除等二级页面/弹窗待后续设计稿输出后另起阶段；当前阶段不得根据列表/卡片稿自行推导弹窗视觉。
4. 删除旧页面级「需求价值跟踪」tab 后，确保新增价值跟踪仍可从项目卡片、列表展开区和空态入口触发。

**阶段 5 执行记录（2026-08-21 17:34）**：

1. 操作日志入口已统一：
   - 左侧目标面板「操作日志」与主内容顶部「操作日志」均打开全量 `OperationLogDialog`。
   - 项目卡片和列表项目行的「日志」打开当前项目维度的 `OperationLogDialog`。
2. 已继续复用现有弹窗和 server actions，未重写二级弹窗视觉：
   - 新增产品目标、新增项目、新增任务、新增价值跟踪。
   - 编辑项目、编辑任务、编辑价值跟踪。
   - 删除项目、删除任务、删除价值跟踪、删除产品目标。
3. 新增价值跟踪入口已覆盖主内容顶部、项目卡片、列表项目行、卡片空态、列表展开区空态，且项目内入口会预选当前项目。
4. 旧页面级「需求价值跟踪」tab 已不再作为主页面渲染路径；价值跟踪能力已迁入项目卡片/列表展开区。
5. 校验结果：
   - `npx eslint "src/app/(authenticated)/quarterly-work/content.tsx"` 通过。
   - `npx tsc --noEmit` 通过。

### 阶段 6：验证与发布

1. 用已导入测试数据核对：
   - 左侧目标汇总数量：全部/单目标下项目数、任务数正确。
   - 状态页签：全部项目、进行中、未启动、已上线、已完成、关闭、延期过滤正确。
   - 双视图：卡片/列表同一筛选条件下项目集合一致。
   - 二级切换：任务与价值跟踪在卡片和列表展开区一致。
   - 空态：无任务、无价值跟踪、hover 创建按钮均可触发正确弹窗。
2. 回归写操作：新增/编辑/删除目标、项目、任务、价值跟踪；操作日志记录仍生成。
3. 运行 `npm run lint`、`npm run build`。
4. 验证 `http://localhost:3003/quarterly-work` 后，使用 `./scripts/refresh-env-after-merge.sh dev:platform` 发布到 3003。

**阶段 6 执行记录（2026-08-21 17:42）**：

1. 已执行校验命令：
   - `npx tsc --noEmit` 通过。
   - `npm run build` 通过；构建中仍有 Turbopack NFT trace warning，来源于 `next.config.ts -> src/server/db/prisma.ts -> src/server/auth/dingtalk.ts -> src/app/login/dingtalk/route.ts`，不阻塞构建。
   - `npm run lint` 未通过，失败来自仓库既有 lint 问题（脚本、KPI、组织、通知、人才、通用删除组件、`types/better-sqlite3.d.ts` 等），本次产品管理改版涉及文件已在阶段 2-5 单独通过 lint。
2. 已执行 `./scripts/refresh-env-after-merge.sh dev:platform`：
   - `npm install` 完成，但提示 16 个依赖漏洞（1 low、3 moderate、12 high），未在本阶段处理。
   - Prisma client 生成成功。
   - `prisma db push --accept-data-loss` 执行后提示数据库 schema 已同步。
   - 脚本内 `npm run build` 成功。
   - 脚本报告 3003 启动成功，但托管 PID 随后退出且日志未显示异常堆栈。
3. 已手动重新启动 `npm run dev:platform`，当前 3003 服务 Ready。
4. `curl -I http://localhost:3003/quarterly-work` 返回 `307 Temporary Redirect` 到 `/login`，说明未登录访问按鉴权预期跳转；刷新脚本日志中曾记录登录态请求 `/quarterly-work?goalId=...&view=list` 返回 `200`。
5. 本阶段未自动回归写操作（新增/编辑/删除目标、项目、任务、价值跟踪），需要在浏览器登录后按测试账号手工验证操作日志是否持续写入。

**页面框架修正记录（2026-08-21 17:49）**：

1. 根据设计稿反馈，已补充外层页面框架调整：
   - `src/components/app-shell.tsx` 对 `/quarterly-work` 单独使用 72px 一级图标栏。
   - 产品管理页不再渲染原有 240px 文字侧边栏和全局顶部横栏。
   - 其他模块仍保留原有 `AppShell` 框架，避免影响全站。
2. 已调整 `QuarterlyWorkShell` 高度和布局：
   - 内容区改为占满 AppShell 剩余高度。
   - 页内左侧目标面板固定为 228px。
   - 右侧工作区内部滚动，减少顶部留白和旧框架感。
3. 校验结果：
   - `npx eslint "src/components/app-shell.tsx" "src/app/(authenticated)/quarterly-work/content.tsx"` 通过。
   - `npx tsc --noEmit` 通过。
   - `curl -I http://localhost:3003/quarterly-work` 返回 `307` 到 `/login`，服务仍可访问。

---

## 接手第一步（新会话粘贴即可）

```text
请阅读 requirements/handoff/quarterly-work/2026-08-21-产品管理改版环境准备与执行计划交接.md，
从「阶段 0：Next.js 16 与现状确认」开始实施产品管理改版。设计稿：
- 卡片/任务：https://mastergo.com/file/201969861780875?devMode=true&page_id=M&layer_id=13%3A15736
- 卡片/价值跟踪：https://mastergo.com/file/201969861780875?devMode=true&page_id=M&layer_id=26%3A4514
- 卡片/价值跟踪空态：https://mastergo.com/file/201969861780875?devMode=true&page_id=M&layer_id=26%3A04348
- 卡片/任务空态：https://mastergo.com/file/201969861780875?devMode=true&page_id=M&layer_id=26%3A09548
- 卡片/任务空态 hover：https://mastergo.com/file/201969861780875?devMode=true&page_id=M&layer_id=26%3A21692
- 列表/任务：https://mastergo.com/file/201969861780875?devMode=true&page_id=M&layer_id=13%3A15239
- 列表/价值跟踪：https://mastergo.com/file/201969861780875?devMode=true&page_id=M&layer_id=26%3A17231
环境：depot-platform 分支，dev:platform 端口 3003，测试数据已导入。
```

---

## 风险与备注

- **Kimi + Claude Code**：长会话 `--continue` 易触发 `tokenization failed`；改版任务请**新开会话**，并设置 `ENABLE_TOOL_SEARCH=false`；MasterGo 任务可精简 MCP。
- **数据库**：若需重导测试数据，先 `cp db/dev.db.pre-product-seed.bak db/dev.db` 或清空产品相关表后再跑导入脚本。
- **文案确认点**：设计稿中视图切换仍写「图表/列表」，产品实现方案建议改为「卡片/列表」；如需完全贴稿，实施前再确认。
