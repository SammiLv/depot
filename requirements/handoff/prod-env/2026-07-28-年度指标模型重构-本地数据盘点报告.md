# 年度指标模型重构 — 本地数据盘点报告

**日期**：2026-07-28  
**数据库**：`db/dev.db`  
**执行方式**：SQLite `-readonly`，未修改任何数据  
**对应计划**：`2026-07-28-年度指标引用改造-库表结构可执行计划.md`

---

## 1. 结论

本地数据量较小，绝大多数数据可以自动迁移：

- 2 个有效部门方案、7 个有效小组方案。
- 6 条部门指标、9 条元指标。
- 16 条有效小组指标副本中，14 条可直接转换为 Assignment，2 条属于 ORPHAN 旧方案重复承接，需确认后跳过。
- 4 条小组季度指标需要归一到“C端产品”共享季度指标。
- `AnnualGoalProgress`、`AnnualGoalRevisionLog` 和 KPI 年度指标引用均为空，迁移关联风险低。

数据处理口径已确认：

1. 3 个 ORPHAN 小组方案按废弃数据处理，不生成 Assignment。
2. “C端产品”Q1/Q2 冲突保留部门级当前值。
3. “创新 ToB 营收”当前值允许清空，后续按元指标/季度数据重新汇总。
4. 没有季度拆解的直属指标继续保留现有当前值。

---

## 2. 方案与组织数据

### 有效方案

- 部门方案：2
- 小组方案：7
- 全部为 2026 年、`APPROVED`、`isActive = true`
- 无同组织同年份重复有效方案

### ORPHAN 小组方案

以下方案的 `ownerOrgNodeId` 已不存在：

- B端组 2026 年度业绩指标：无有效指标
- C端组 2026 年度业绩指标：承接“C端产品”，权重 50
- 采购组 2026 年度业绩指标：承接“公共平台”，权重 20

当前组织树中对应名称已经变为：

- B端业务组
- C端业务组
- 采购业务组

当前新方案中已经存在：

- C端业务组 → C端产品
- 采购业务组 → 公共平台

确认执行：

- 不把旧 ORPHAN 方案再生成新的 Assignment。
- 以当前“业务组”方案为准。
- 迁移完成后软删旧 ORPHAN 方案及其指标副本。
- B端组旧方案无指标，可直接进入待软删清单。

---

## 3. 指标与承接迁移

### 数据量

- 有效部门指标：6
- 有效元指标：9
- 有效小组指标副本：16
  - 直接承接部门指标：4
  - 承接元指标：12
- 已软删小组指标副本：7

### 自动匹配结果

- 4 条直接承接均能通过“所属部门 + 年份 + metricCode”唯一匹配部门指标。
- 12 条元指标承接的 `sourceMetricId` 均有效。
- 未发现同一 TEAM plan 重复承接同一有效 sourceMetric。
- 未发现失效 sourceMetric 引用。

除 ORPHAN 旧方案的两条重复承接外，其余有效小组指标可以自动生成 Assignment。

预计最终有效 Assignment 数量为 14（旧 C端组、采购组的两条重复承接不迁移）。

---

## 4. 季度指标归一

### 数据量

- 有效部门季度指标：26
- 已软删部门季度指标：21
- 有效小组季度指标：4
- 有效小组季度指标全部属于 C端业务组承接的“C端产品”

### 目标值

部门侧与小组侧目标值完全一致：

- Q1：3000
- Q2：7500
- Q3：10500
- Q4：9000

### 当前值冲突

- Q1：
  - 部门共享候选值：2187（2026-07-01 更新）
  - C端业务组值：2165（2026-07-14 更新）
- Q2：
  - 部门共享候选值：2619（2026-07-01 更新）
  - C端业务组值：2836（2026-07-14 更新）
- Q3、Q4：双方均为 0，无冲突

迁移规则已确认：

- Q1 保留部门级当前值 2187。
- Q2 保留部门级当前值 2619。
- Q3/Q4 双方一致，保留现有部门规范行。
- 4 条 TEAM 季度行合并到 `sourceMetricId = C端产品` 的共享季度行后软删。
- 迁移脚本仍输出冲突报告，记录被舍弃的小组值，便于审计。

---

## 5. 当前值对账

### 元指标

9 条元指标的 `currentValue` 均与其有效部门季度当前值合计一致。

“C端产品”当前权威值为：

```text
2187 + 2619 + 0 + 0 = 4806
```

若采用小组侧较新值，则变为：

```text
2165 + 2836 + 0 + 0 = 5001
```

### 有元指标的部门指标

- AI应用指标：存储值 2，元指标合计 2，一致。
- 创新 ToC 用户增量：存储值 8034，元指标合计 8034，一致。
- 创新 ToB 营收：存储值 800000，元指标合计 0，不一致。

已确认“创新 ToB 营收”当前值可以清空。迁移时将其按有效元指标/季度数据重新汇总；当前数据下结果为 0。

### 无元指标的部门指标

以下指标存在年度 `currentValue`，但没有有效季度指标：

- 单位拓展业绩分值：86
- 平台稳定性改进项交付：5
- 跨部门数据服务支撑：3

已确认迁移时原值保持不变。只有在后续创建/更新季度进度时，才按共享季度数据重算，不能因“当前没有季度行”自动归零。

---

## 6. 历史与外围引用

- `AnnualGoalProgress`：0 条
- `AnnualGoalRevisionLog`：0 条
- `PersonalKpiItem`：204 条
- KPI → 年度指标引用：0 条
- KPI → 季度指标引用：0 条
- 无失效 KPI 年度/季度引用

结论：

- 本地库无需迁移 Progress 引用，但迁移脚本仍须支持生产环境存在数据的情况。
- `AnnualGoalRevisionLog` 本地可安全删除；生产仍需先跑相同审计。
- KPI 当前不依赖年度指标 id，可降低 TEAM metric 清理风险。

---

## 7. 进入 Phase 1 前的确认项

```text
☑ 旧 B端组/C端组/采购组方案按已废弃方案处理，不生成重复 Assignment
☑ C端产品 Q1 最终 currentValue = 2187（部门值）
☑ C端产品 Q2 最终 currentValue = 2619（部门值）
☑ 创新 ToB 营收 currentValue 清空并按元指标/季度数据重算
☑ 无季度行的直属指标继续保留现有 currentValue
```

上述确认已完成，本地数据满足进入“加法迁移”的条件。

