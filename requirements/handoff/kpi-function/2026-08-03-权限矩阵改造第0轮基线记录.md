# 权限矩阵改造第 0 轮基线记录

## 1. 基线范围

- 执行日期：2026-08-03
- 工作区：`depot-coordination/depot-KPI`
- 分支：`depot-KPI`
- 基线提交：`7df7f1be11e0836f971a14b0e0603c3b35ca6326`
- 对应方案：`2026-08-03-权限矩阵全选与一键同步全局改造方案.md`
- 本轮原则：只记录、备份和验证，不修改权限业务逻辑，不修改数据库结构，不清理任何现有数据。

## 2. Git 改动基线

执行第 0 轮前，工作区已有 KPI 审批流改造的未提交内容：

- 已跟踪文件改动：16 项。
- 未跟踪目录或文件入口：11 项。
- 这些既有改动整体作为权限矩阵改造的起始基线；后续审查必须区分“既有 KPI 审批流改动”和“权限矩阵新增改动”。
- 本轮没有提交、合并或推送代码。

## 3. 现有实现基线

### 3.1 前端草稿矩阵

三类权限已有相互独立的完整草稿状态：

- 菜单权限：`draftRoleMenuCells`
- 年度指标权限：`draftAnnualGoalCells`
- KPI 权限：`draftKpiCells`

现有普通保存、取消和单单元格编辑语义继续保留。

### 3.2 保存入口

- `saveRoleMenuPermissions`
- `saveAnnualGoalRolePermissions`
- `saveKpiRolePermissions`

### 3.3 旧单格同步入口

- `applyRoleMenuPermissionToAllDepartments`
- `applyAnnualGoalPermissionToAllDepartments`
- `applyKpiPermissionToAllDepartments`

第 0 轮确认旧单格同步入口仍在。本轮及后续第一、第二轮先保留，整体同步验收完成后再决定是否移除。

### 3.4 数据结构结论

当前表结构能够支持全选、清空和完整矩阵全局同步：

- 菜单权限：`RoleMenuPermission`
- 年度指标权限：`RoleAnnualGoalPermission`
- KPI 权限：`OrgPermissionGrant`

本次权限矩阵改造不新增 Prisma migration。

## 4. 测试数据库备份

备份文件：

`db/backups/permission-matrix-baseline-20260803/dev-baseline.db`

校验结果：

- SQLite `integrity_check`：`ok`
- SHA-256：`ee2f621bc0cbe5dcc112ac78b2b1d8798fb93961dd6383367e40a39041343e23`

该备份只用于当前测试环境的改造对照和恢复，不是生产数据库备份。

## 5. 数据数量基线

| 数据集 | 行数 | 备注 |
| --- | ---: | --- |
| `OrgNode` | 14 | 根节点 1、部门 2、团队 11 |
| `User` | 24 | 组织和用户保护数据 |
| `MenuPermission` | 8 | 菜单定义 |
| `RoleMenuPermission` | 34 | SYSTEM 32、DEPARTMENT 2 |
| `AnnualGoalPermission` | 5 | 年度指标权限定义 |
| `RoleAnnualGoalPermission` | 18 | SYSTEM 16、DEPARTMENT 2 |
| KPI ROLE 授权 | 0 | `moduleKey=KPI`、`subjectType=ROLE` |
| KPI USER 授权 | 0 | `moduleKey=KPI`、`subjectType=USER` |

权限状态补充：

- 菜单权限：SYSTEM 开启 28/32，DEPARTMENT 开启 2/2。
- 年度指标权限：SYSTEM 开启 16/16，DEPARTMENT 开启 2/2。
- 当前测试库没有 KPI 显式角色授权和成员授权；后续 KPI USER 授权保护测试必须在临时测试库中构造专用样本，不能依赖当前开发库已有数据。

## 6. 保护数据指纹

以下指纹由排序后的稳定字段生成，用于后续核对是否发生误改：

| 数据集 | SHA-256 |
| --- | --- |
| 组织节点 | `fe663efd9ebfe86536526101c8bd4e96ce55d9b6fd31d0fa5bf96f2bfe78c70d` |
| 用户 | `e87fc8b58fedc47cd5531dcf2546d75b6a2820cb20edd1f4217cf239f479300c` |
| 菜单角色权限 | `2b768aad62683efb0582fc7c77bec162c526039082526080955fcff03f925857` |
| 年度指标角色权限 | `83076886f0fb264d125f0386d66d035273c04f4cf2a95057212ab57d62d84842` |
| 组织权限授权 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |

测试执行后重新计算，以上指纹均未变化。

## 7. 自动化测试基线

| 测试 | 结果 |
| --- | --- |
| 年度指标权限规则测试 | 8/8 通过 |
| KPI 核心测试 | 38/38 通过 |
| KPI 数据库全链路集成测试 | 1/1 通过，空库应用 35 个 migration |
| KPI 生产迁移测试 | 3/3 通过 |
| 生产编排、回滚和 PowerShell 合约测试 | 通过 |
| Next.js 生产构建 | 通过 |

说明：生产编排测试首次在受限沙箱内执行时，Turbopack 因无权绑定内部端口而中断；在允许构建进程正常运行的环境中重新执行后全部通过。构建仍存在项目原有的 `next.config.ts` NFT trace 警告，不属于本轮新增问题。

## 8. 第 0 轮结论

第 0 轮完成，可以进入第一轮“权限矩阵批量选择能力”开发。后续必须遵守以下边界：

1. 不修改组织、用户和非目标权限数据。
2. 全选/清空只先修改前端草稿，点击保存后才持久化。
3. 菜单核心入口必须保持锁定，前后端双重保护。
4. KPI 完整同步只处理角色授权，成员显式授权必须保留。
5. 旧单格同步入口暂时保留。
6. 每轮完成后重新核对保护数据数量与指纹。
