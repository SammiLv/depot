# 首页工作台问题交接（2026-07-15）

## 1. 问题性质

这是一个**非计划遗留问题**。

这里说的“计划”，指：

- `requirements/handoff/kpi-function/2026-07-06-KPI动态审批链改造计划.md`

该计划本身的动态审批链改造主体已经完成；当前遗留的是**首页工作台（Dashboard）展示口径问题**，不属于审批链主流程本身未完成。

---

## 2. 问题结论

当前首页工作台对 KPI 待处理统计，和真实 KPI 动态审批链状态**不完全一致**。

具体表现：

- 某张 KPI 单据已经真实进入 `PENDING_FINAL_REVIEW`
- 且当前登录人就是这一步的实际终审人
- 但首页工作台 `待我评分` 卡片仍显示：
  - `0 项终审待处理`

这说明：

- **KPI 审批链主流程是通的**
- **详情页状态是对的**
- **首页工作台统计口径有偏差**

---

## 3. 本次实际验证依据

本次是在运行中的应用里做的真实验证，不是只读代码判断。

### 已实际跑通的新审批链单据

验证单据：

- `cmra0vi2m001w5hyrjgc5amao`（设计组成员A，2026 Q3）

该单据我已实际按下面路径跑通：

1. 自评提交
2. 组长退回
3. 成员重新提交
4. 组长通过
5. 主管通过
6. 终审通过
7. 最终完成

中间关键节点已实际确认：

- 主管通过后，单据真实进入 `PENDING_FINAL_REVIEW`
- 详情页状态真实显示为：`终审`
- 流程进度条真实显示：`终审 / 当前阶段`
- 审批人真实显示为：`产品部主管`

也就是说：

- 服务端状态推进没问题
- 详情页阶段展示没问题

---

## 4. 首页工作台问题的具体现象

在上面这张单据已经进入 `PENDING_FINAL_REVIEW` 的时刻，用 `manager` 账号查看首页工作台，页面文案实际显示：

```text
待我评分 = 2
1 项组长评分待处理 · 1 项主管评分待处理 · 0 项终审待处理
```

但此时真实情况应该至少包含：

- 1 项终审待处理

所以这里存在明确不一致。

---

## 5. 当前判断

当前问题应归类为：

- **首页工作台统计/展示问题**
- **不是 KPI 动态审批链主流程问题**
- **不是详情页阶段识别问题**
- **不是旧单据 fallback 问题**

换句话说：

- 审批链流转本身已经可用
- 问题集中在 Dashboard 对 KPI 各阶段待处理数量的汇总口径

---

## 6. 可能涉及位置

优先关注：

- `src/server/dashboard/dashboard-query.ts`

建议同时联动复核：

- `src/server/kpi/kpi-query.ts`
- `src/app/(authenticated)/kpi/content.tsx`

原因：

- 详情页对 `PENDING_FINAL_REVIEW` 的判断已经是正确的
- KPI 主查询层也已经能识别 `canFinalReview`
- 首页工作台统计没有同步表现出这个终审待处理量

所以更大概率是 Dashboard 聚合逻辑与当前审批链 pending step 口径没有完全对齐。

---

## 7. 建议接手处理顺序

建议后续按这个顺序处理：

1. 先复现问题
   - 用 `manager` 账号
   - 找一张已进入 `PENDING_FINAL_REVIEW` 的 KPI 单据
   - 打开 `/dashboard`
   - 核对 `待我评分` 卡片里的三段文案

2. 再对照详情页和数据库确认真实状态
   - 详情页是否显示 `终审`
   - `PersonalKpi.status` 是否为 `PENDING_FINAL_REVIEW`
   - `PersonalKpiApprovalStep` 当前 `PENDING` step 是否确实落在 `FINAL`

3. 最后修 Dashboard 聚合口径
   - 核对是否只按 `status` 聚合
   - 核对是否漏掉“当前 pending step 的 approverId = 当前人”这一层判断
   - 核对 `PENDING_FINAL_REVIEW` 是否被误归到别的统计项

---

## 8. 一句话结论

`2026-07-06-KPI动态审批链改造计划.md` 对应的动态审批链主体已完成；当前遗留的是**首页工作台问题**：Dashboard 对 `PENDING_FINAL_REVIEW` 的待处理统计与真实审批链状态不一致，需要单独修正展示/聚合口径。
