# KPI 权限统一技术方案执行进度回顾（2026-07-15）

## 1. 回顾对象

本次回顾对象是：

- `requirements/KPI管理权限统一技术方案.md`

目标不是重新写方案，而是回答：

- 这份技术方案现在执行到哪一步了
- 哪些部分已经真正落到代码
- 哪些部分已经完成真实运行验证
- 哪些部分还停留在后续阶段

---

## 2. 当前总判断

如果按这份技术方案自己的分阶段路线来判断，当前进度可以概括为：

- **阶段一：权限底座** —— 已完成
- **阶段二：KPI 查看 / 模板 / 初始化接入统一权限模型** —— 已完成
- **阶段三：评分流程重构** —— 已基本完成，且实际落地深度已超过方案当时的第一版预期
- **阶段四：年度指标权限迁移** —— 未完成

换句话说：

这份技术方案在 **KPI 侧** 已经不是“设计中”或“半落地”，而是已经进入 **主体落地完成态**；当前剩余的更多是：

- 局部回归验证
- 配套展示层问题
- 年度指标模块后续迁移

---

## 3. 已完成的方案部分

### 3.1 权限底座已完成

技术方案里要求的统一权限底座已经落地，核心包括：

- 权限能力项枚举
- scope 范围模型
- 通用授权表 `OrgPermissionGrant`
- 默认授权 seed
- 统一权限解析层

当前对应代码：

- `db/prisma/schema.prisma`
- `db/prisma/seed.ts`
- `src/server/permissions/permission-constants.ts`
- `src/server/permissions/permission-query.ts`
- `src/server/permissions/permission-resolver.ts`

方案要求的 KPI 能力项现在都已经存在并投入使用：

- `VIEW_KPI`
- `VIEW_KPI_TEMPLATE`
- `MANAGE_KPI_TEMPLATE`
- `TOGGLE_KPI_TEMPLATE`
- `INITIALIZE_KPI`
- `SCORE_SELF`
- `SCORE_LEADER`
- `SCORE_MANAGER`
- `SCORE_FINAL`

方案要求的统一 scope 模型也已经存在：

- `SELF`
- `NODE`
- `SUBTREE`
- `ALL`

并且已经在 resolver 中具备优先级比较与 where 条件翻译能力。

---

### 3.2 组织与权限页面接入已完成

技术方案要求把 KPI 权限纳入“组织与权限”页面统一管理。

这一部分已经完成，当前页面中已经具备：

- 年度指标权限
- KPI 权限

并列展示与保存。

对应代码：

- `src/app/(authenticated)/organization/content.tsx`
- `src/server/organization/actions.ts`

当前页面已经支持：

- KPI 权限矩阵展示
- 草稿态与变更检测
- 保存 KPI 权限配置
- 与年度指标权限并列呈现

这部分已经符合方案第 10 章提出的页面接入目标。

---

### 3.3 KPI 查看、模板、初始化已切到统一权限模型

技术方案第 8 章提出，第一阶段先把以下能力切到统一权限模型：

1. KPI 列表查看
2. KPI 详情查看
3. KPI 模板列表查看
4. KPI 模板新建/编辑
5. KPI 模板启用/禁用
6. 季度 KPI 初始化

当前执行情况：

#### KPI 列表 / 详情查看

已完成：

- `VIEW_KPI` 已进入 `kpi-query.ts`
- 列表 where 与详情访问范围都已经走统一 resolver

对应代码：

- `src/server/kpi/kpi-query.ts`

#### KPI 模板查看

已完成：

- `VIEW_KPI_TEMPLATE` 已进入模板列表查询与可见性筛选

对应代码：

- `src/server/kpi/kpi-query.ts`

#### 模板维护 / 启停

已完成：

- 模板维护能力已走 `MANAGE_KPI_TEMPLATE`
- 模板启停能力已走 `TOGGLE_KPI_TEMPLATE`

对应代码：

- `src/server/kpi/actions.ts`

#### 季度 KPI 初始化

已完成：

- 初始化能力已走 `INITIALIZE_KPI`
- 初始化人群裁剪已走统一权限范围
- 模板匹配与用户范围控制已经接入统一权限模型

对应代码：

- `src/server/kpi/actions.ts`

所以如果只对照技术方案第 8 章，这一整块已经可以认为是完成态。

---

### 3.4 KPI 评分权限按阶段能力控制已完成

技术方案要求评分能力拆分为：

- `SCORE_SELF`
- `SCORE_LEADER`
- `SCORE_MANAGER`
- `SCORE_FINAL`

当前这套能力已经不只是存在于底座里，而是已经进入 KPI 详情页和评分动作主逻辑。

对应代码：

- `src/server/kpi/kpi-query.ts`
- `src/server/kpi/actions.ts`

当前已实现：

- 详情页按钮可用性按阶段能力判断
- 评分动作服务端按当前阶段映射对应 ability
- 不再只是“能进入详情就能操作”

这意味着技术方案里“评分权限不能只校验查看权限”的要求，在 KPI 主流程中已经落地。

---

### 3.5 评分流程重构已实际落地，且超出方案第一版预期

技术方案第 12 章原本提出的是二阶段策略：

- 第一版：运行时解析
- 第二版：审批链快照

但当前实际落地情况比方案写的第一版更进一步。

当前已经具备：

- 审批链解析逻辑
- 审批链快照落单据
- 按 pending step 审批人控制当前可操作人
- 新旧单据双路径兼容

对应代码：

- `src/server/kpi/approval-chain.ts`
- `src/server/kpi/actions.ts`
- `src/server/kpi/kpi-query.ts`

并且这不是只读代码判断。

本轮已经对运行中的应用做过真实验证：

- 成员自评提交
- 组长退回
- 成员重新提交
- 组长通过
- 主管通过
- 终审通过
- 最终完成

同时还验证了：

- 退回后重新提交可以续接原审批链
- 无审批链旧单据仍能走 fallback

因此，技术方案里“评分流程重构”这部分，现在已经不是“待开始”，也不是“仅做到运行时解析”，而是已经进入 **真实可运行状态**。

---

## 4. 已基本完成但仍建议继续复核的部分

### 4.1 KPI 模板分组展示与权限关系

技术方案第 9 章强调：

- `全部`：展示部门全部模板
- 小组 tab：仅展示命中组长/组员的模板
- 部门级模板不进入小组 tab
- 原始适用范围展示不能被“可见命中对象”污染

当前从代码上看，这套结构已经具备：

- `scopeDepartmentOrgNodeIds`
- `scopeTeamIds`
- `scopeUserIds`
- `groupTeamIds`

都已经在查询层中输出。

说明：

- 服务端建模已经到位
- 模板分组不再只是前端拍脑袋推断

但这部分业务规则最复杂，仍建议后续继续做一轮角色回归验证，而不只靠代码判断。

---

### 4.2 KPI 初始化边界场景

技术方案第 15 章要求验证：

- Team 节点用户仍能命中 department 节点模板
- 初始化范围受 `INITIALIZE_KPI` 控制
- 初始化结果与模板匹配逻辑正确

从代码结构上看，初始化链路已经接到统一权限模型上；
但如果问“是否所有模板命中边界都已经做过完整人工回归”，当前还不能说完全收口。

所以这一块更准确的状态是：

- 主实现已完成
- 边界验证仍建议继续补做

---

## 5. 当前尚未完成的方案部分

### 5.1 年度指标权限迁移未完成

技术方案第 13 章最后一阶段写的是：

- 将年度指标权限逐步迁移到统一授权模型

当前状态看：

- 年度指标权限已经和 KPI 权限一起出现在“组织与权限”页面中
- 但不能据此判断“年度指标模块已经彻底迁移完毕”

也就是说：

- **KPI 权限统一方案在 KPI 侧已基本完成**
- **方案对年度指标模块的后续迁移目标还未完成**

这是这份技术方案最明确的未完成项。

---

## 6. 当前技术方案执行后的遗留问题

### 6.1 首页工作台统计口径问题

这不是技术方案主体未完成，但它是当前最明确的配套遗留问题。

现象：

- 某 KPI 单据已经真实进入 `PENDING_FINAL_REVIEW`
- 详情页阶段展示正确
- 审批链状态推进正确
- 但首页工作台 `待我评分` 统计没有正确体现终审待处理数量

这个问题已经单独记录在：

- `requirements/handoff/kpi-function/2026-07-15-首页工作台问题交接.md`

应归类为：

- 配套展示层问题
- 不影响技术方案主体“权限统一 + 审批链落地”完成判断

---

## 7. 按技术方案阶段重新映射的当前结论

### 阶段一：权限底座

**状态：已完成**

已完成内容：

- 新增权限枚举
- 新增统一授权表
- 新增默认授权 seed
- 新增权限解析层

### 阶段二：KPI 查看 / 模板 / 初始化接入

**状态：已完成**

已完成内容：

- KPI 列表查看接 `VIEW_KPI`
- KPI 详情查看接 `VIEW_KPI`
- 模板列表查看接 `VIEW_KPI_TEMPLATE`
- 模板编辑接 `MANAGE_KPI_TEMPLATE`
- 模板启停接 `TOGGLE_KPI_TEMPLATE`
- 季度 KPI 初始化接 `INITIALIZE_KPI`

### 阶段三：评分流程重构

**状态：已基本完成**

而且实际落地深度已超过技术方案第一版预期，当前已经落到：

- 审批链解析
- 审批链快照
- 审批步骤控制可操作人
- 新旧单据兼容
- 真实流转验证通过

### 阶段四：年度指标权限迁移

**状态：未完成**

这是当前技术方案里最明确仍保留在后续阶段的内容。

---

## 8. 一句话结论

`KPI管理权限统一技术方案.md` 在 **KPI 侧** 已经基本进入“主体落地完成态”：统一权限底座、组织与权限页面接入、KPI 查看/模板/初始化权限统一、评分能力拆分与审批链重构都已落地，并且关键流程已经做过真实运行验证；当前剩余的主要是**年度指标模块后续迁移**以及**首页工作台这类配套展示层问题**，而不是 KPI 权限统一方案本身还卡在主实现阶段。
