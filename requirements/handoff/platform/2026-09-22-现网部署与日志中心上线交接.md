# 现网部署与日志中心上线交接

**环境**：`C:\appdeploy\depot`（生产，本机 `next start`）  
**交接日期**：2026-09-22  
**GitHub**：`https://github.com/SammiLv/depot`（branch `main`）

---

## 一、待闭环（开发环境为主）

1. **日志中心前端**  
   开发机已有 `/logs` 完整页面，需确认已 push 到 **`SammiLv/depot` 的 `main`**（`src/app/(authenticated)/logs/`）。

2. **`AuditEvent` 正式 Prisma migration**  
   仓库 schema 含 `AuditEvent`，migrations 仍缺对应文件。开发补 migration 后 merge；现网库表已存在时，合并后 migrate 需核对无破坏性变更。

3. **现网本地已 commit、待 push**（勿在现网继续堆 commit）  
   - `fix(nav)`：`system-bootstrap` 注册 **logs-center**；`app-shell` 按 permission **code** 映射侧栏（`logs-center` → `/logs`，`todos` → `/statistics`）  
   - 已删除一次性脚本 `scripts/add-audit-center-menu.ts`（菜单靠 bootstrap + 组织与权限矩阵维护）

---

## 二、产品与权限约定

- 统一入口：**日志中心**，路由 **`/logs`**；**不要**单独 `/audit` 菜单。  
- 菜单数据：`MenuPermission` code **`logs-center`**，path **`/logs`**。  
- 侧栏可见性：`allowedMenus` + `app-shell`；权限变更后菜单缓存约 **300s**，必要时重启服务。  
- 默认仅 **ADMIN** 系统级可访问（bootstrap）；其它角色在矩阵勾选后保存或完整同步至所有部门。

---

## 三、日志中心 merge 后现网操作

```bash
cd C:/appdeploy/depot
git pull
bash scripts/depot-prod.sh deploy
```

- 日常：`bash scripts/depot-prod.sh pull`  
- 已手动 pull、需补构建/迁移：`bash scripts/depot-prod.sh deploy`  

**上线前备份**：`db/dev.db`、`.env`。

**菜单**：登录触发 `ensureInitialSystemBootstrap()` 会 upsert **logs-center**；矩阵中确认授权即可。

**验收**：矩阵仅有 **日志中心 `/logs`**；管理员侧栏有入口；`/logs` 为查询页而非 404。

---

## 四、开发自测命令

```bash
git fetch origin
git rev-parse HEAD origin/main
git ls-tree -r origin/main --name-only | findstr /i "authenticated\\\\logs"
```

---

## 五、相关文件

| 路径 | 说明 |
|---|---|
| `scripts/depot-prod.sh` | `pull`、`deploy` |
| `scripts/log-deployment.ts` | 部署审计写入 |
| `src/server/bootstrap/system-bootstrap.ts` | 系统菜单含 logs-center |
| `src/components/app-shell.tsx` | 侧栏与 permission code 映射 |
| `src/server/audit/*` | 日志查询后端 |

---

**文档路径**：`C:\appdeploy\prodenv-issues-handoff\2026-09-22-现网部署与日志中心上线交接.md`
